# -*- coding: utf-8 -*-
"""
Developer tooling.  NOT shipped -- this package is excluded from the frozen
build (spec/10-deployment.md).  It reads the corpus, which never ships either.

    python -m tsubasa.dev corpus --stat
    python -m tsubasa.dev corpus --split
    python -m tsubasa.dev corpus --verify-split
"""
